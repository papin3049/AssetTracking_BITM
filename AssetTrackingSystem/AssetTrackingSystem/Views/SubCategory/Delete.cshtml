
@model AssetTracking.Model.SubCategory
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="col-lg-10 content">
    <div class="col-md-5">
        <div class="panel panel-default">
            <div class="panel-heading">
               Delete

            </div>
            @using (Html.BeginForm("Delete", "SubCategory", FormMethod.Post, new { @class = "form-horizontal" }))
            {
                <div class="panel-body">

                    <div>

                        @Html.LabelFor(m => m.SubCategoryName)

                        @Html.TextBoxFor(m => m.SubCategoryName, new { @placeholder = "Name", @class = "form-control" })
                        <br />
                    </div>
                    <div>
                        @Html.LabelFor(m => m.SubCategoryCode)

                        @Html.TextBoxFor(m => m.SubCategoryCode, new { @placeholder = "Code", @class = "form-control" })
                        <br />
                    </div>
                    <div>
                        @Html.LabelFor(m => m.Description)

                        @Html.TextBoxFor(m => m.Description, new { @placeholder = "Description", @class = "form-control" })
                        <br />
                    </div>


                    <div>
                        <button id="autoclosable-btn-success" class="btn btn-primary btn-success btn-block" type="submit">Update</button>
                    </div>
                    <br />

                </div>

            }

            <div id="autoclosable-btn-success" class="alert alert-success">
                @if (ViewBag.Message != null)
                {
                    <strong>@ViewBag.Message</strong>
                }

            </div>

        </div>
    </div>
</div>

@section Scripts
    {
    <script src="~/scripts/button.js"></script>
}




